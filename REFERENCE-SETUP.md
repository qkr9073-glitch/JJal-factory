# 레퍼런스 채널 자동수집 세팅 (최초 1회, 약 10~15분)

핸들만 넣으면 다른 채널(예: @selectionmgz)의 게시물을 **공식 API로 자동 수집**해
스타일·템플릿 학습까지 잇기 위한 준비. Meta 공식 기능(business_discovery)이라
계정 정지 걱정 없는 정상 루트이고, **전부 무료**입니다.

> 왜 필요한가: 지금 쓰는 "인스타 로그인 방식" 토큰은 이 기능이 막혀 있음(실측 확인).
> "페이스북 로그인 방식" 토큰에서만 열려서 아래 3단계가 필요.

## 1) 페이스북 페이지 만들기 (3분)
1. https://www.facebook.com/pages/create 접속 (사장님 개인 페북 계정으로 로그인)
2. 페이지 이름: 아무거나 (예: `캥거루쇼츠`) / 카테고리: `교육` → **페이지 만들기**
3. 프로필 사진·게시물 등은 **안 채워도 됨** (빈 페이지 OK, 공개될 일 없음)

## 2) 인스타그램 연결 (2분)
1. 방금 만든 페이지 → **설정** → **연결된 계정 (Linked accounts)** → **Instagram**
2. **계정 연결** 클릭 → **kangarooshort** 로 로그인/승인
   (메시지 접근 허용 여부는 아무거나 — 이 기능엔 영향 없음)

## 3) 토큰 발급 — 그래프 API 탐색기 (5~10분)
1. https://developers.facebook.com/tools/explorer 접속
2. 오른쪽 상단 **Meta 앱**: `jjal-factory` 선택
3. **User or Page**: `User Token` 선택
4. **권한(Permissions) 추가** — 아래 4개를 검색해서 전부 추가
   (Meta 공식 business_discovery 문서 기준, 2026-08 검증):
   - `instagram_basic`
   - `instagram_manage_insights`  ← 공식 문서상 필수
   - `pages_read_engagement`
   - `pages_show_list`
5. **Generate Access Token** 클릭 → 페북 로그인 창 → **1)에서 만든 페이지**와
   **kangarooshort** 선택해서 허용
6. 생성된 **긴 토큰 문자열 복사** → Claude에게 채팅으로 전달하면 끝

## 이후는 자동 (Claude 작업)
- 받은 토큰을 장기 토큰(60일)으로 교환 + 페이지·IG 비즈니스 ID 조회 → config.json 저장
- @selectionmgz 실조회 테스트 (팔로워/게시물/이미지 수집 확인)
- 서버에 "핸들 입력 → 자동 수집 → 스타일·템플릿 학습" 기능 빌드

## 참고
- 조회 대상은 **프로페셔널(비즈니스/크리에이터) 계정만** 가능. 개인 계정이면
  API가 바로 알려주므로 미리 고민할 필요 없음.
- 기존 인스타 자동 업로드(인스타 로그인 방식 토큰 2개)는 그대로 유지 —
  이번 토큰은 "남의 채널 조회" 전용으로 추가되는 것.
- 문제 생기면: 탐색기에서 권한 추가가 안 보이면 앱 대시보드에서 `jjal-factory`에
  "Facebook 로그인" 제품을 추가한 뒤 다시 시도.
